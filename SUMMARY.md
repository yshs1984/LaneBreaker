# 連射シューター 開発サマリー

対象ファイル: `shooter-game.html`(単一HTMLファイル、外部依存なし。Canvas 2D + Vanilla JS)

広告でよくある「複数レーンで敵が迫ってくる連射シューティング」を目指して、対話しながら段階的に仕様を詰めてきた。以下、現状の仕様と設計意図をまとめる。

## 全体構成

- 画面はスマホ縦持ち想定。`<canvas>`1枚にゲーム全体を描画。
- HUD(スコア・ウェーブ・HPバー・バフ表示)はDOM要素をcanvasの上に重ねている。
- ゲームループは `requestAnimationFrame`。`dt` は前フレームからの経過時間を16.67msで正規化した値(最大2にクランプ)。

## レーンシステム(最重要の設計軸)

- `LANE_COUNT = 5`。`laneX(i)` で各レーンの中心x座標を計算。
- `ENEMY_LANES = [0,2,4]`(敵専用、背景を薄い赤で着色)
- `POWERUP_LANES = [1,3]`(アイテム専用、背景を薄い水色で着色)
- 自機は連続移動ではなく **レーン単位のスナップ移動**。ドラッグ/タップ位置に一番近いレーンへ `player.targetLane` をセットし、`player.x` は毎フレーム補間(イージング)でそこへ近づく。当たり判定用の `player.lane` は即座に更新される(見た目の移動が追いつく前でも判定はレーン切り替え時点で発生)。

## 敵システム

### 敵レーンに沿った直進
敵は `ENEMY_LANES` のいずれかに固定で発生し、レーン内で軽くサイン波の横揺れ(`swayPhase`)はするが、レーンをまたぐことはない(ハンター敵を除く)。

### 継続スポーン(ウェーブ待機なし)
- 以前は「全滅したら次ウェーブ」方式だったが、「安全地帯ができてしまう」問題があったため、**時間経過で継続的にスポーンし続ける方式**に変更済み。
- `spawnCooldown` が0になるたびに `spawnEnemyRow()` を呼び、`spawnInterval()`(ウェーブが進むほど短縮、下限16フレーム)でリセット。
- `wave` 自体も `waveTimer`(約8秒)で自動的に加算される、敵全滅を待たない時間ベースの難易度指標。

### 4段階の敵ティア(`ENEMY_TIERS`)
| ティア | 解禁ウェーブ | HP倍率 | サイズ倍率 | 速度倍率 | 被弾ダメージ | スコア倍率 | hunter |
|---|---|---|---|---|---|---|---|
| normal | 1 | 1 | 1.0 | 1.0 | 14 | 1 | false |
| hard(装甲) | 2 | 5 | 1.25 | 0.62 | 22 | 3 | false |
| elite(エリート) | 5 | 9 | 1.35 | 0.78 | 26 | 5 | true |
| titan(タイタン) | 9 | 18 | 1.6 | 0.5 | 34 | 9 | true |

- `pickTier()` がウェーブ数に応じた加重ランダムで1体ごとにティアを決定(`normal`の重みは徐々に下がり、上位ティアの重みはウェーブが進むほど上がる)。
- `hunter: true` のティア(elite/titan)は、`shiftTimer` が切れるたびに自機のいるレーンと違えば `telegraph`(黄色い点滅で予告、約22フレーム)を経てから実際にレーンを切り替える。**「同じレーンに居座り続ければ勝てる」問題への対策**として導入。

### 被弾判定
敵が画面下端(`H()-40`)に到達したとき、**自機の現在レーンと敵のレーンが一致する場合のみ**ダメージが発生する(違うレーンなら素通り)。シールドがあれば1回分消費して無効化。

## パワーアップ / レベルシステム

### 供給レーン専用・専用タイマー
- `POWERUP_LANES` にのみ、`itemSpawnTimer`(約5秒間隔)で自動的にアイテムが降ってくる。**敵の撃破ドロップが主ではない**(撃破時は8%の低確率でボーナスドロップのみ、`maybeDropItem`)。
- アイテムを取り逃すと画面外へ消えて終わり(猶予演出はまだ入れていない)。

### アイテム種別(`ITEM_TYPES`)
- `rapid`(連射・青)/`spread`(拡散弾・紫)/`power`(攻撃力・赤)/`shield`(シールド・緑)/`heal`(回復・橙)

### 永続レベルアップ + 自動減衰
- `rapid` / `spread` / `power` は **時間制限なしの永続バフ**。拾うたびに `levels.{key}` が+1され、上限は `LEVEL_CAP = { rapid:6, spread:5, power:8 }`。
  - `rapid`: 発射間隔が `9 - levels.rapid*1.1`(下限2.5フレーム)に短縮。
  - `spread`: レベル数分だけ角度違いの弾ペアが追加発射される。
  - `power`: 1発あたりのダメージが `1 + levels.power`。
- `shield` は時間ではなく**回数ストック**(`shieldCharges`、上限3)。敵の突破を1回無効化するたびに1消費。
- `heal` は即時HP+30(ストック・レベルの概念なし)。
- **自動減衰**: `levelDecayTimer`(初期値 `LEVEL_DECAY_INTERVAL = 2400`フレーム ≒ 40秒)が0になるたびに `rapid/spread/power` のレベルを1ずつ下げる(0未満にはしない)。下がった時は画面上部に「強化が弱まった…アイテムを取ろう」というトースト通知(`addToast`)が出る。**「アイテムを取り続けないと徐々に弱くなる」ことで供給レーンへの往復を強制する狙い**。

## HUD / 演出

- `#buffRow` にレベル表示(`Lv1〜`)とシールド残数(`×n`)をアイコンで表示。
- 敵ヒット時は白フラッシュ、撃破時はパーティクル(ティアカラー)。
- プレイヤーの現在レーンをうっすらハイライト表示、レーン境界に薄い縦線。
- トースト用に `toasts` 配列(`{text,color,life,y}`)を追加済み(画面上部中央にフェードアウト表示)。

## これまでのユーザーフィードバックの流れ(設計判断の根拠)

1. 「明確に3レーンにして」「アイテムも欲しい」→ レーン固定化 + アイテム導入。
2. 「敵レーンとパワーアップレーンを分けて」「パワーアップしないと押しつぶされる」「硬い敵が欲しい」→ 5レーン(敵3/供給2)に分離、硬い敵(装甲)を追加。
3. 「序盤はいいが中盤は必ず負ける」「アイテムを時間制限なしにして」「火力もレベルアップ式に」→ バフを時間制限→永続レベル制へ変更。
4. 「強くなると真ん中から動かなくても勝てる」「もっと強い敵を段階的に」「アイテムレベルが時間で落ちる仕掛けも」→ 4ティア制+hunter(狙いを自機レーンへ切り替える敵)導入、レベル自動減衰を追加。

## 未調整・要検討ポイント(次にやるならここ)

- レベル減衰の間隔(40秒)・hunterの予告時間(22フレーム)・出現間隔などは体感で未調整。実プレイでバランス確認が必要。
- タイタン(最上位ティア)はまだ大きな見た目上のインパクト(専用モーション・専用HPバー演出など)が薄い。
- ゲームオーバー画面・リトライ導線は最小限(スコアとウェーブ数のみ表示)。
- サウンド・BGM・振動フィードバックは未実装。
- コンボ/連続撃破ボーナスなどのスコアリング拡張は未着手。
- モバイル実機でのタッチ判定・パフォーマンス(星背景の描画など)は簡易実装のまま。
