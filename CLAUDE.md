# Lane Breaker

ビルド不要の単一ファイルCanvasゲーム。`index.html` → `version.js` → `game.js` の順に読み込まれる。

## アーキテクチャ境界

- `game.js` は**トップレベルスコープ**(IIFEには包まれていない。`shooter`リポジトリはIIFEで包んでいるが、lane_breakerは現状そうなっていない — 実態に合わせてここに明記している。新規に大規模なリファクタリングをして揃える必要はない)
- `game.js` 内部は `// ---- セクション名 ----` というコメントで区切られたセクション構成(game state／input／update／draw)。新規コードは対応するセクションに置く
- `version.js` は `.github/workflows/version-bump.yml` がPRの作成・更新のたびに自動で書き換える生成ファイル。**手動で編集しない**

## 命名規約

- 敵・アイテムの生成は `makeEnemy()` / `makeItem()` に集約されている。新しい生成経路(デバッグAPIなど)を追加するときも、直接オブジェクトリテラルを書かずにこれらを再利用する
- コメントは日本語

## 禁止事項・注意点

- `version.js` を手動編集しない(自動生成ファイル)
- 検証用の `window.__t` は `game.js` 末尾の `if (DEBUG)` 内にある**常設API**。検証のたびにアドホックなフックを足したり消したりせず、足りない操作があれば常設APIに追加する
- `?debug=1` のデバッグモード(`DEBUG`定数)は通常プレイに一切影響しないことが前提
- コードを変更したら `node tools/verify.mjs` で回帰シナリオを流す(Node 20以上が必要)。`node --check game.js` は構文チェックのみで、意味的な退行は検出しない

詳細仕様(レーン構成・敵/アイテム仕様・デバッグ手順など)は [`docs/spec.md`](docs/spec.md) を参照。全体を読む必要はなく、作業内容に対応する章だけ読めばよい。
